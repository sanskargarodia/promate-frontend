# Backend API base URL (no trailing slash). Local: docker-compose backend on port 8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Stripe publishable key (test mode). Used at checkout in Phase 4.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
